/**
 * *****************************************************************************
 *
 * is promise object
 *
 * *****************************************************************************
 */

export const isPromise: boolean = v => v 
  && typeof v === "object" 
  && typeof v.then === "function";
