/**
 * *****************************************************************************
 *
 * is json string value
 *
 * *****************************************************************************
 */

const jsonRegExp = /^[\x20\x09\x0a\x0d]*(\[|\{)/;
export const isJSON: boolean = (v: string) => jsonRegExp.test(v);
