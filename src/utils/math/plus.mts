/**
 * *****************************************************************************
 *
 * plus algorithem
 *
 * *****************************************************************************
 */

export function plus (x: number, y: number): number {
  return Number(x) + Number(y);
}
