/**
 * *****************************************************************************
 *
 *
 * *****************************************************************************
 */

export const average = (...args) => args.reduce((a, b) => a + b) / args.length;
